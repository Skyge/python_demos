全双工模式，意味着通信两端都可以发送并接收消息，并且二者相互独立。

由于input()函数具有阻塞作用，即客户端先发完一条信息，只能等待另一端的消息发过来才能继续发消息。

此时我们可以将输入与接收分开来，将接收的函数从主线程里抓出来丢到另一个线程里单独运行，而想要实现这个功能，就要考虑多线程了。

import threading便可导入多线程。

但是该版本version1.3须在在CMD中运行。
