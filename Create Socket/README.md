1.运行环境是Win7+Python3.6.1(其他环境大同小异)
2.先运行server.py，再运行client.py。
3.此版本(version1.0)运行有诸多的限制：
    1)对话最开始只能由客户端发起；
    2)半双工聊天。意思就是，当建立一个连接且服务开始后，只有一个人能打字，而另一个参与者在得到输入消息提示之前必须等待消息。并且，一旦发送者发送了一条消息，在他能够再次发送消息之前，必须等待对方回复。其中，一位参与者将在服务器一侧，而另一位在客户端一侧。
    3)当然一次发的内容不宜过多。
