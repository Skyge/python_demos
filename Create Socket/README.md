1.运行环境是Win7+Python3.6.1(其他环境大同小异)
2.先运行server.py，再运行client.py。
3.此版本(version1.0)运行有诸多的限制：
    1)对话最开始只能由客户端发起；
    2)无论是客户端还是服务端，对话是一次单向的，即以Enter键为界限，Enter键前的内容可以送达到对方，但是当对方无再次发送消息时，你是无法再次向对方发送消息的！这时你输入的内容会在对方再次发来消息时回复过去。
    3)当然一次发的内容不宜过多。
